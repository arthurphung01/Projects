/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    images: {
        domains: ["gizstclsluomvldliezz.supabase.co"],
        
    },
}

module.exports = nextConfig